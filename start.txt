##Adding new changes as part of git tagging
Test Git Quick Start Demo<br>
"$ git diff master origin/master" - comparing local repository(master) with remote repository
(git hub)
It compares the left side with right side

